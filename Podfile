# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Ko' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Ko

  target 'KoModelMacOSTests' do
    inherit! :search_paths
    # Pods for testing
  end
  pod 'SwiftyUtils'
  pod 'SCLAlertView'
end

target 'KoModel' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KoModel

end

target 'KoModelMacOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KoModelMacOS
end
